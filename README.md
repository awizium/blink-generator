# Blink Generator

## Что это

Навайбкоденный простенький веб-интерфейс для генерации картинок и общения с AI-моделями.
Работает через API ключ от blink.new.
Один HTML файл, без зависимостей, всё локально в браузере.

## Быстрый старт

    git clone https://github.com/awizium/blink-generator.git

### Что нужно

- API ключ от blink.new
- Python
- Любой браузер (Chrome, Firefox, Edge)

## Запуск

### Windows

Запустите файл start.bat.
Если Python не установлен, скрипт сообщит об этом.

### macOS / Linux

Откройте терминал в папке проекта и выполните:

    chmod +x start.sh
    ./start.sh

Команду chmod достаточно выполнить один раз — она даёт файлу разрешение на запуск.

### Вручную (любая ОС)

Если скрипты не работают, откройте терминал в папке проекта и выполните:

    python -m http.server 8080

Если команда не найдена — попробуйте python3 вместо python.

Затем откройте в браузере:

    http://localhost:8080/blink_image_gen.html

## Как пользоваться

1. Запустите start.bat или start.sh — откроется браузер
2. Справа в настройках нажмите + Новый рядом с API Ключ
3. Введите название и ваш ключ от Blink
4. Выберите модель (для картинок — вкладка Картинки)
5. Напишите промпт и нажмите Enter

## Где хранятся данные

Всё локально в браузере:

- Настройки и ключи — localStorage
- Картинки — IndexedDB (любой размер)

Ничего не уходит на сторонние серверы. Запросы идут напрямую к blink.new.

## Файлы проекта

    blink-generator/
    ├── blink_image_gen.html   — основное приложение (один файл, всё внутри)
    ├── start.bat              — запуск на Windows
    ├── start.sh               — запуск на macOS / Linux
    └── README.md              — документация
